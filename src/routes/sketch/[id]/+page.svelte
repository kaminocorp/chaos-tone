<script lang="ts">
	// Stub — same Workbench as `/`. Sketch hydration (loading the row and
	// applying its parameter snapshot to the stores) lands once Phase 4
	// (Supabase) and Phase 7 (createParamStore) are in place.
	import Workbench from '$lib/components/workbench/Workbench.svelte';
</script>

<Workbench />
